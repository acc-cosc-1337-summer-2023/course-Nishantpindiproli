#include <string>
using namespace std;

#ifndef REPETITION_H
#define REPETITION_H

/*
Write prototype for function(s)
*/
double get_gc_content(const string reference);
string get_reverse_string(const string reference);
string get_dna_complement(const string reference);

#endif